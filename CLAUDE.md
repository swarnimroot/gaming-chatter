# gaming-chatter — instructions for Claude

Personal gaming-news aggregator. Daily ingest from 15 news sites + 10 subreddits + 6 YouTube channels → Monday-morning weekly exec summary + live dashboard. Local-only, single user.

## Status

Latest work: **2026-06-11 — degraded-run diagnosis + cost model CORRECTED (~$1.15/night, ~$450/yr — NOT the earlier $0.30/$120 claim) + per-phase cost breakdown on `/runs` + recovery runs**. The overnight PC restart produced a `degraded` catch-up daily (id=5, $1.25): `embed: 343 failed` (Ollama not yet up — local/free, self-heals) + 6 YT feeds transient-404'd (all recovered same day; no Reddit → no rolloff loss). Diagnosis: **no cost bug** — $1.25 was a NORMAL night. **Cost model corrected:** real volume is **~351 new items/night (range 145–691, ~2,450/week)** from `items.published_at`; the old "34–100/night" assumption came from a run that saw `new=34` only because an interrupted run 1h earlier had already ingested the batch. Measured **$0.00327/item all-in** → typical night **$1.15**, annual **~$450 incl. weekly Opus**; $1,000/yr would need 2.4× volume. **User accepted up to $500/yr**; levers (merge region-tag into enrich; Batch API 50%) identified, deliberately NOT built. **Per-phase cost breakdown BUILT:** `cost.record()` takes `phase`; all 9 call sites tagged (`enrich`/`yt_prescreen`/`game_tag`/`cluster_label`/`region_tag`/`release_extract`/`synthesis`/`exec_summary`); priced rollup persisted to `details_json._cost_by_phase` (no schema change) + Cost-column tooltip shows the split sorted by $. Verified: unit tests (attribution, nested exclusivity, leak-fold) + real-DB `_finish_run` round-trip. **Recovery (Option B) COMPLETE:** `ingest_only` ok in 18.5s (new=204, 0 errors — ingest is ALWAYS ~20s, scales with source count not news volume; the old "10–20 min" estimate was wrong); `enrich_only` ok in 53 min / **$0.5970** (enrich 180 ok / 24 skipped / 0 failed; embed 541/541 — **backlog 0, unenriched 0**; first production per-phase row: `enrich $0.5650 (180 calls) · yt_prescreen $0.0320 (39)`, matching the $0.00327/item model). **UI follow-ups:** `/runs` Cost column reduced to one decimal (tooltip keeps 4-dp per-phase detail); ingest confirm dialog corrected to "~1 min". **Server is DOWN at session end (user choice — starts it themselves: `python -m uvicorn app.main:app --port 8001`, no `--reload`; startup catch-up self-heals a late start).** Tonight's 23:00 auto-daily is the first fully-metered scheduled run; Monday's daily chains the first metered weekly Opus synthesis (last estimated number in the cost model). Prior session (2026-06-10, Phase 4 — activation + safety): scheduler ACTIVATED (was INERT), confirm dialogs on `/runs` triggers, alert-banner wording softened. **Scheduler is now ACTIVE** (`SCHEDULER_ENABLED=1` added to `.env`; uvicorn restarted on `127.0.0.1:8001` WITHOUT `--reload`; boot log confirmed `APScheduler started: daily=23:00 local; weekly chained off daily`; startup-catchup ran and found the last daily NOT overdue (<24h) → no immediate run / no surprise spend). The first real automated daily fires **tonight 23:00 CST** and will populate the first real `/runs` cost row. **Confirm dialogs added to all 7 `/runs` "Run now" buttons** (`hx-confirm`, HTMX-native, no JS build step) — each states what the job does + realistic runtime + whether it spends Anthropic $; realistic times sourced from `job_runs` history + operational knowledge (Daily 4–6h, Enrich+embed 3–5h [Whisper-dominated], Ingest 10–20m, Cluster 5–15m [Sonnet $], Synthesis 5–10m, Weekly 5–10m, Release refresh 2–5m); eval writes + the exec-summary "Generate" are DELIBERATELY NOT confirmed (eval = cheap/local/reversible save-on-change; exec-summary generates only on cache miss for a fraction of a cent then caches forever, and `hx-confirm` can't tell cached from uncached so it would nag on every free reopen). **Alert-banner wording softened** ("erroring" → "needs attention") in `app/templates/_alert_banner.html`. **No dev work outstanding** — remaining items are user actions only: (a) watch tonight's first auto-daily and confirm the cost row populates; (b) OPTIONAL — set an Anthropic Console monthly spend cap. Phase 3d (archive) remains OPTIONAL/deferred, off critical path. The prior automation-wiring + cost-meter context (still accurate): daily cron moved **07:00 → 23:00 local (CST)** so the brief is ready in the morning, and the standalone `Mon 07:30` weekly cron was **removed** — the weekly now runs **chained inline at the end of each daily run** (`_previous_week_needs_synthesis()` → `run_weekly_extension(week_id=...)`, RLock re-entrant), so it always runs AFTER that night's ingest + self-heals a missed run (`app/main.py`, `app/services/jobs.py`). Decision: **no timezone migration** ("Option A") — kept UTC week definitions; 23:00 Central already briefs the just-closed UTC week. **Nightly full-corpus backfill warts fixed (cost-critical):** the daily's region backfill is scoped to items enriched *this run* (not the whole untagged corpus — was re-billing ~10,495 Haiku calls/night forever) and `enrich_after_fetch` re-enriches only items that just got bodies (`enrich_pending(item_ids=...)`, not the whole skipped backlog — was re-running Whisper over the entire YT backlog); full-corpus region pass is now manual-only (`scripts/backfill_region.py`), which also got a `""`-sentinel idempotency fix. Typical-night cost with the fixes ≈ **$1.15 (~$450/yr)** — corrected 2026-06-11 from the earlier wrong $0.30/$120 estimate — vs ~$1,500/yr extra WITHOUT the fixes. **r/GamesIndustry removed** (frozen `.rss`, 0 items since 2026-05-21; source id=24 + 13 items/raw/enrich + 5 run_log rows purged) — NOT `GamesIndustry.biz` (news, id=9, KEPT). Corpus: **13,297 items / 31 active sources** (as of 2026-06-11) / 5 synthesized `weekly_reports` rows (W19–W23; W24 clusters-only). Earlier-today operator-console work (health band, earned `degraded` + floors, reconcile-on-boot, per-source recency grid, nav/styling, synthesis retry-cap) remains current. Reddit Jun 3–6 permanently lost (RSS rolloff); pre-3c.35 history (W17/W18) lost from the 3c.34 corpus wipe. The daily scheduler is now **ACTIVE** (`SCHEDULER_ENABLED=1`; see top of this paragraph). **Per-run cost meter is BUILT** (`app/services/cost.py`): a thread-local, exclusive-semantics stack accumulator captures each Anthropic response's real `usage` (input/output + cache tokens), prices it per model (Haiku $1/$5, Sonnet $3/$15, Opus $4/$20 per MTok; cache read 0.1x, cache write 1.25x; unknown model → $0 + warning), and shows actual $ per `JobRun` on `/runs` (Cost column, tokens in tooltip; pre-meter rows render `—`). The inner-frame attribution keeps inline-nested weekly Opus tokens off the daily row (no double-count); `record()` is wired at all 9 Anthropic call sites; 3 nullable columns (`input_tokens`/`output_tokens`/`cost_usd`) added via the idempotent `_migrate_job_runs_columns` ALTER pattern in `app/db/init.py`. Verified: imports compile, migration idempotent, stack unit test passes; it still shows `—` for historical rows until the first live run populates a real cost row. **Scheduler ACTIVE; no dev work outstanding — remaining items are user actions** (watch tonight's first auto-daily and confirm the cost row populates; optional Anthropic Console spend cap). For phase-by-phase history and rationale, see `docs/SESSION_LOG.md`, `docs/DECISIONS.md`, and `CHANGELOG.md`.

## Read first
- `docs/PRD.md` — what we're building and why
- `docs/ARCHITECTURE.md` — how it works
- `docs/DECISIONS.md` — locked choices + rationale
- `docs/TASKS.md` — phased build plan + status
- `docs/OPEN_QUESTIONS.md` — unresolved items
- `docs/SESSION_LOG.md` — prior-session handoff notes

## Hard architectural constraints — do not violate without explicit approval

- **Single Python process.** FastAPI + APScheduler + scrapers-lib + Ollama HTTP + Anthropic SDK in one app. Do not introduce a worker queue, separate scheduler service, Docker container, or split process model.
- **SQLite single-file storage.** No Postgres, no DuckDB, no Redis, no dedicated vector DB. Embeddings stored as BLOBs; cosine similarity in numpy.
- **HTMX + Jinja frontend.** No React/Vue/Svelte. No JavaScript build step. The UI template handed over from claude.ai/design will be ported into Jinja partials.
- **LLM split (updated 2026-05-12 — see DECISIONS.md "2026-05-12 (later)"):** **embeddings on Ollama** (`nomic-embed-text`, 768-dim); **per-item enrichment on Anthropic Haiku 4.5**; **cluster labels on Anthropic Sonnet 4.6**; **synthesis + critic pass on Anthropic Opus 4.7**. Do not introduce OpenAI, llama.cpp direct, or other providers. The pre-2026-05-12 lock said "Ollama local for per-item work, Anthropic API for synthesis only" — that lock was overridden after the qwen2.5:7b structured-output sample exposed concrete quality issues (Reddit-handle leak in entities, movie tagged with game genres) and 4–5× runtime regression vs Haiku.
- **scrapers-lib is an external Python dependency** at `..\scrapers-lib`. Use `tier1` modules: `rss` (handles BOTH news-site feeds AND Reddit subreddit feeds — see DECISIONS.md 2026-05-07), `youtube`, `article`. The `tier1.reddit` (PRAW) module is currently NOT used because the Reddit API application was rejected; see `docs/OPEN_QUESTIONS.md`. Do not modify scrapers-lib from this project.
- **Personal-local only.** No auth, no TLS, no cloud, no multi-user. Listens on `localhost`.

## Session conventions

- Major decisions → append a dated entry in `docs/DECISIONS.md`.
- End of every working session → append to `docs/SESSION_LOG.md`: what was done, where we left off, what's blocked.
- Update `docs/TASKS.md` checkboxes as work moves.
- New unknowns / deferred items → `docs/OPEN_QUESTIONS.md`.

## Out of scope (deferred to "Later")

- Push delivery (email/Discord) — after core phases ship
- Multi-user, accounts, sharing
- Backfill of historical data — accept cold start
- Mobile-specific UI

## scrapers-lib at a glance

Located at `C:\Users\AW-testing\Downloads\Workspace\scrapers-lib`. Python lib. `tier1` provides RSS (used for both news sites AND Reddit subreddits via Reddit's public RSS endpoint), Reddit-PRAW (currently unused — API rejected, may flip back if reapproved), YouTube + transcripts, generic article extraction (justext). `core` provides rate limiting, caching, robots.txt, attribution, scheduler. `tier2/tier3` (laptop/retail) are unused for this project.
