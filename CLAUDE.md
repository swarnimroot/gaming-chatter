# gaming-chatter — instructions for Claude

Personal gaming-news aggregator. Daily ingest from 15 news sites + 10 subreddits + 6 YouTube channels → Monday-morning weekly exec summary + live dashboard. Local-only, single user.

## Status

Latest work: **2026-06-15 — Reddit unauthenticated-RSS rate-limit fix (1 req/60s per IP) + daily-scheduler hardening for Modern Standby + first metered scheduled dailies & weekly observed**. **Reddit RSS rate fix (`app/services/scrapers.py`):** Reddit's unauthenticated `.rss` endpoint dropped to **1 request / 60s per IP** (a single shared bucket across ALL subreddits) sometime between 06-11 and 06-13 — PROVEN via response headers (`x-ratelimit-used:1 / x-ratelimit-remaining:0.0 / x-ratelimit-reset:59`; isolated request = 200, so not an IP block; 6s spacing still 429'd 9/10). Fix: a process-wide **65s rate-gate** (`_is_reddit_url` + `_reddit_rate_gate` via `threading.Lock`+`monotonic`) plus a `_fetch_reddit_rss` 429 backstop that sleeps `x-ratelimit-reset+2s` and retries once (Reddit sends `x-ratelimit-reset`, NOT `Retry-After`), wired into `fetch_source`'s rss branch; non-reddit RSS + YouTube unchanged. Adds ~10 min of idle wall-clock (no CPU/$) to ingest for 10 subreddits; `/runs` "Ingest only" confirm text updated "~1 min" → "~10 min". **Scheduler hardening (`app/main.py`, `app/services/jobs.py`, `app/routers/runs.py`, `app/db/models.py`):** root cause of the 2026-06-11 23:00 daily NOT firing was Modern Standby (S0) — screen-off suspends uvicorn and APScheduler's default `misfire_grace_time=1s` discarded the late job. Fix: cron now uses `misfire_grace_time=None` and targets a new `run_daily_pipeline_scheduled` guard (skips if a daily started <12h ago with status running/ok/degraded; a recent `failed` does NOT block). Added `POST /runs/trigger-scheduled/daily` for an optional Windows Task Scheduler poke; `JobRun.triggered_by` now also documents `'schtask'`. The Task Scheduler artifacts (`scripts/daily_trigger.xml`, `scripts/trigger_daily.ps1`) are **DEFERRED/UNUSED** — the in-process cron fired on time 3/3 nights so the OS poke is not enabled (XML-encoding registration also failed once); kept ready-to-enable. **Observed runtime (live, from `job_runs`):** scheduler is ACTIVE and fired on time 3 nights — id=8 (night of 06-12) degraded $1.18 new=275; id=9 (06-13) degraded $0.37 new=107; id=10 (06-14) degraded $0.38 new=104 — all `degraded` ONLY because of the 9 Reddit 429s (everything else OK; the post-fix nights will be the true volume baseline since 104–107 were depressed by the Reddit outage). First chained **metered weekly**: id=11 ok **$0.484492**, synthesized 2026-W24 (chained off id=10) — this was the cost model's last *estimated* number, now measured. **20 unit tests pass** (8 new `tests/test_scheduled_guard.py`, 8 new `tests/test_reddit_gate.py`, plus pre-existing). Two new dated DECISIONS entries (2026-06-15: Reddit rate-gate; scheduler/Task-Scheduler). **Remaining: user actions only** — optional Windows Task Scheduler enable if a future missed-daily recurs; optional Anthropic Console monthly spend cap. **Server start is the user's** (`python -m uvicorn app.main:app --port 8001`, no `--reload`; startup catch-up self-heals a late start). Prior session (2026-06-11): degraded-run diagnosis (no cost bug) + **cost model CORRECTED to ~$1.15/night (~$450/yr)** from `items.published_at` (mean 351 items/night, ~2,450/week; $0.00327/item all-in; user accepted up to $500/yr) + **per-phase cost breakdown on `/runs`** (`cost.record()` takes `phase`; all 9 call sites tagged; priced rollup persisted to `details_json._cost_by_phase`; tooltip shows the split) + recovery runs (ingest 18.5s, enrich+embed 53 min/$0.5970, backlog 0). The automation context (still accurate): daily cron 23:00 local (CST) so the brief is ready in the morning; the standalone `Mon 07:30` weekly cron was removed — the weekly runs **chained inline at the end of each daily** (`_previous_week_needs_synthesis()` → `run_weekly_extension(week_id=...)`, RLock re-entrant); **no timezone migration** ("Option A", UTC week definitions kept). **Nightly full-corpus backfill warts fixed (cost-critical):** region backfill scoped to items enriched *this run*; `enrich_after_fetch` re-enriches only items that just got bodies; full-corpus region pass manual-only (`scripts/backfill_region.py`, `""`-sentinel idempotency). **r/GamesIndustry removed** (frozen `.rss`; source id=24 + 13 items/raw/enrich + 5 run_log rows purged) — NOT `GamesIndustry.biz` (news, id=9, KEPT). Corpus: **13,783 items / 31 active sources / 6 synthesized `weekly_reports` rows (W19–W24)** as of 2026-06-15. Reddit Jun 3–6 permanently lost (RSS rolloff); pre-3c.35 history (W17/W18) lost from the 3c.34 corpus wipe. **Per-run cost meter is BUILT** (`app/services/cost.py`): thread-local exclusive-semantics stack accumulator captures each Anthropic response's real `usage`, prices per model (Haiku $1/$5, Sonnet $3/$15, Opus $4/$20 per MTok; cache read 0.1x, write 1.25x; unknown model → $0 + warning), shows actual $ per `JobRun` on `/runs`; inner-frame attribution keeps inline-nested weekly Opus tokens off the daily row; wired at all 9 call sites; 3 nullable columns via `_migrate_job_runs_columns`. **Scheduler ACTIVE; no dev work outstanding.** For phase-by-phase history, see `docs/SESSION_LOG.md`, `docs/DECISIONS.md`, and `CHANGELOG.md`.

## Read first
- `docs/PRD.md` — what we're building and why
- `docs/ARCHITECTURE.md` — how it works
- `docs/DECISIONS.md` — locked choices + rationale
- `docs/TASKS.md` — phased build plan + status
- `docs/OPEN_QUESTIONS.md` — unresolved items
- `docs/SESSION_LOG.md` — prior-session handoff notes

## Hard architectural constraints — do not violate without explicit approval

- **Single Python process.** FastAPI + APScheduler + scrapers-lib + Ollama HTTP + Anthropic SDK in one app. Do not introduce a worker queue, separate scheduler service, Docker container, or split process model.
- **SQLite single-file storage.** No Postgres, no DuckDB, no Redis, no dedicated vector DB. Embeddings stored as BLOBs; cosine similarity in numpy.
- **HTMX + Jinja frontend.** No React/Vue/Svelte. No JavaScript build step. The UI template handed over from claude.ai/design will be ported into Jinja partials.
- **LLM split (updated 2026-05-12 — see DECISIONS.md "2026-05-12 (later)"):** **embeddings on Ollama** (`nomic-embed-text`, 768-dim); **per-item enrichment on Anthropic Haiku 4.5**; **cluster labels on Anthropic Sonnet 4.6**; **synthesis + critic pass on Anthropic Opus 4.7**. Do not introduce OpenAI, llama.cpp direct, or other providers. The pre-2026-05-12 lock said "Ollama local for per-item work, Anthropic API for synthesis only" — that lock was overridden after the qwen2.5:7b structured-output sample exposed concrete quality issues (Reddit-handle leak in entities, movie tagged with game genres) and 4–5× runtime regression vs Haiku.
- **scrapers-lib is an external Python dependency** at `..\scrapers-lib`. Use `tier1` modules: `rss` (handles BOTH news-site feeds AND Reddit subreddit feeds — see DECISIONS.md 2026-05-07), `youtube`, `article`. The `tier1.reddit` (PRAW) module is currently NOT used because the Reddit API application was rejected; see `docs/OPEN_QUESTIONS.md`. Do not modify scrapers-lib from this project.
- **Personal-local only.** No auth, no TLS, no cloud, no multi-user. Listens on `localhost`.

## Session conventions

- Major decisions → append a dated entry in `docs/DECISIONS.md`.
- End of every working session → append to `docs/SESSION_LOG.md`: what was done, where we left off, what's blocked.
- Update `docs/TASKS.md` checkboxes as work moves.
- New unknowns / deferred items → `docs/OPEN_QUESTIONS.md`.

## Out of scope (deferred to "Later")

- Push delivery (email/Discord) — after core phases ship
- Multi-user, accounts, sharing
- Backfill of historical data — accept cold start
- Mobile-specific UI

## scrapers-lib at a glance

Located at `C:\Users\AW-testing\Downloads\Workspace\scrapers-lib`. Python lib. `tier1` provides RSS (used for both news sites AND Reddit subreddits via Reddit's public RSS endpoint), Reddit-PRAW (currently unused — API rejected, may flip back if reapproved), YouTube + transcripts, generic article extraction (justext). `core` provides rate limiting, caching, robots.txt, attribution, scheduler. `tier2/tier3` (laptop/retail) are unused for this project.
